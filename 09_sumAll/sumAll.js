const sumAll = function(a, b) {
    // if negative or 0, send message ERROR
    if ((a < 0 || b < 0) || !Number.isInteger(a) || !Number.isInteger(b) || typeof a != "number" || typeof b != "number") {
        return "ERROR";
    };

    // check if which int is bigger
    if (a > b) {

    }

    // bigger int gets count down from until variable equals lower int

    // sum up all integers
};

// Do not edit below this line
module.exports = sumAll;
